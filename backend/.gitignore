node_modules
# Keep environment variables out of version control
.env

/generated/prisma

coverage/
dist

# One-off migration script backups — contain real patient health data (LGPD Art. 11)
migrate-anamnesis-fields-backup-*.json